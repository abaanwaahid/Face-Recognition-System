# Face-Recognition-System
This project is a Real-Time Face Recognition System built using Python and OpenCV. It captures facial data through a webcam, trains a model using the LBPH (Local Binary Patterns Histogram) algorithm, and recognizes faces in real-time.  The system supports incremental learning, meaning you can add new faces without retraining the model from scratch.
